{
  "collectionName": "components_element_sizes",
  "info": {
    "displayName": "size",
    "description": ""
  },
  "options": {},
  "attributes": {
    "paddingTop": {
      "type": "enumeration",
      "enum": [
        "sm",
        "md",
        "lg"
      ],
      "default": "md"
    },
    "paddingRight": {
      "type": "enumeration",
      "enum": [
        "sm",
        "md",
        "lg"
      ],
      "default": "md"
    },
    "paddingBottom": {
      "type": "enumeration",
      "enum": [
        "sm",
        "md",
        "lg"
      ],
      "default": "md"
    },
    "paddingLeft": {
      "type": "enumeration",
      "enum": [
        "sm",
        "md",
        "lg"
      ],
      "default": "md"
    },
    "marginTop": {
      "type": "enumeration",
      "enum": [
        "sm",
        "md",
        "lg"
      ],
      "default": "md"
    },
    "marginBottom": {
      "type": "enumeration",
      "enum": [
        "sm",
        "md",
        "lg"
      ],
      "default": "md"
    },
    "width": {
      "type": "enumeration",
      "enum": [
        "sm",
        "md",
        "lg",
        "full"
      ],
      "default": "md"
    },
    "height": {
      "type": "enumeration",
      "enum": [
        "auto",
        "sm",
        "md",
        "lg",
        "window"
      ],
      "default": "md"
    },
    "verticalAlignment": {
      "type": "enumeration",
      "enum": [
        "top",
        "center",
        "bottom"
      ],
      "default": "top"
    }
  }
}
